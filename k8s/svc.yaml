apiVersion: v1
kind: Service
metadata:
  labels:
    run: backend
  name: backend
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 8000
    nodePort: 30001
  selector:
    run: backend
  type: NodePort
status:
  loadBalancer: {}
